/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "1000logos.net",
            "upload.wikimedia.org",
            "encrypted-tbn0.gstatic.com",
        ],
    },
};

module.exports = nextConfig;
